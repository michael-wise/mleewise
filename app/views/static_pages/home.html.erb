<main class="columns">
	<div class="flex1 center">
		<%= "content" %>
	</div>
	<div class="flex3 rows">
		<div class="center em12">EventBrite Tech Events</div>
		<%= @events = Event.last(5)
			render partial: 'techinmiami/events', collection: @events, as: :event 
		%>
	</div>

</main>