<section class="rows">
    <% @posts.each do |post| %>
      <section>
          <h2><%= post.title %></h2>
          <h6><%= post.created_at.strftime('%b %d, %Y') %></h6>
          <%= raw(post.body).truncate(358) %>
          <%= link_to "READ MORE", post_path(post) %>
      </section>
    <% end %>

</section>